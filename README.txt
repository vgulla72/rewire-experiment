In a virtual environment, install dependencies using the following command:

* pip install -r requirements.txt


To run the chat app, within the virtual env:
* Modify the chat_app.py to include the path for the resume in line #42
* python chat_app.py


